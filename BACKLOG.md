# Backlog & Known Issues

The standing register of everything outstanding and every known bug. This is
the canonical list — other docs link here rather than restating.

> Picking this work up fresh? Read **[`HANDOFF.md`](HANDOFF.md)** alongside
> this. It carries the ordering, the setup, and the traps; this file is the
> inventory.

**Last reviewed:** 2026-09-22 (local session, second pass) · **Pipeline:** running daily, **22 of 22 CBs current** — zero failing scrapers, zero stale. BCR was discontinued on 2026-09-22; NBP is collected by the self-hosted runner (R4). Discovery verified against all 23 live sites on 2026-09-20; NBP is the only scraper that returns nothing, which is correct and expected on a headless runner.

Severity: **P1** breaks or silently corrupts data · **P2** degrades reliability
or correctness · **P3** cleanup, cosmetics, tech debt.

---

## Open bugs

| ID | Sev | Item | Evidence |
|---|---|---|---|
| ~~B1~~ | ~~P1~~ | ~~**Fed scraper charset bug**~~ — **fixed forward**; see *Fixed this cycle*. The 54 already-stored statements remain corrupted until backfilled (spend decision, below). | Measured over `statements.parquet` with `[âÃ][\x80-\x9f]` |
| ~~B2~~ | ~~P2~~ | ~~**`build_methodology_data.py` is not in the daily workflow**~~ — **fixed 2026-09-21.** It is now a step after build_site.py, and `git add docs/data/` already covers its output. methodology.json had last been built on 2026-05-17. Original note: **not in the daily workflow**, so `methodology.json` drifts from the data indefinitely. | `daily_update.yml` step list |
| ~~B3~~ | ~~P2~~ | ~~Its input CSV was deleted.~~ — **recovered from `b139b91^` and committed**, since the workflow now runs the script in CI where nothing else would produce it. Original note: Its input `notebooks/scale_analysis_results.csv` was deleted, so it takes a skip branch and ships pre-built figures from a removed notebook. Recoverable at `b139b91^`. | skip branch in that script |
| ~~B4~~ | ~~P2~~ | ~~**Gemini and DeepSeek do not retry JSON-parse failures on short statements.**~~ — **fixed**; every path now goes through `BaseScorer._call_and_parse`. See *Fixed this cycle*. | `gemini_scorer.py`, `deepseek_scorer.py` |
| ~~B5~~ | ~~P2~~ | ~~**`rbnz.py` catches only `RuntimeError`**~~ — **fixed**, and it had already cost two outages (2026-09-18 and 09-19). Discovery also had no fallback at all, which is where a failure costs all 154 statements instead of one. See *Fixed this cycle*. | `rbnz.py:187` |
| ~~B6~~ | ~~P2~~ | ~~**`fed.py` has illusory redundancy.**~~ — **fixed**; the three advertised strategies are now backed by a real second path (`probe_from_calendar` over deterministic statement URLs), and discovery raises rather than returning `[]`. See *Fixed this cycle*. | `fed.py:92-93, 108-113` |
| ~~B7~~ | ~~P3~~ | ~~Stray `gemini_thinking` rows in `sentences.parquet`.~~ — **11,196 rows removed 2026-09-21**; published doc_score checksum unchanged and re-running the ensemble moved zero of 4,407 statements. The 9,963 `openai` rows are deliberately kept as the GPT-4o baseline. Original note: Stray `gemini_thinking` rows linger in `sentences.parquet` from a removed model variant. | parquet string dictionary |
| ~~B8~~ | ~~P3~~ | ~~Dead artefacts.~~ — **both removed 2026-09-21.** v.json in particular guaranteed a one-line diff on every build, which is noise sitting in front of trap 3.1. Original note: `snapshot.json` is built every run and never fetched; `v.json` is written and never read (`app.js` cache-busts with `Date.now()`). | `build_site.py`, `app.js:41` |
| ~~B9~~ | ~~P3~~ | ~~`python-bcb` and `scikit-learn` declared but unused.~~ — **removed**, with the lock recompiled and diffed pin-by-pin to confirm nothing else moved (five packages gone including scipy/joblib/threadpoolctl, no version changes). | `requirements.txt` |
| ~~B10~~ | ~~P2~~ | ~~**BCR's listing discovery is dead.**~~ — **moot: BCR discontinued 2026-09-22** (I7). Original note: The probe used to hide it by reporting a find every run; as of 2026-09-20 it correctly reports a MISS (see B11), so BCR's discovery is now visibly near-zero. The listing itself is still dead and this stays open under I7. Original evidence: **BCR's listing discovery is dead and masked by the probe.** A full scan of 67 English pages plus the Spanish fallback returns **0 refs** (`BCR listing en/pg0..66: 0 dated + 0 deferred`); only the calendar probe finds anything. The CB therefore looks "self-healing" while one of its two paths is gone — the same illusory-redundancy shape as B6. Detection can't fire because the probe keeps discovery non-zero. | `update.py --cb bcr` run 2026-08-22 |

| ~~B11~~ | ~~P2~~ | ~~**A date-derived statement id lets one junk document block a real one.**~~ — **fixed 2026-09-22.** A block now names the document it rejected, by URL, and refuses only that one. The 21 entries written before this keep their old meaning — block the id outright — because reinterpreting them would silently unblock genuine rejections, and a test asserts each still does. Original note: `data/blocked_ids.json` is consulted before any scraper runs and nothing ever expires from it. CBRT's over-inclusive discovery (B12) auto-blocked a liquidity-management press release dated 2026-09-17; the genuine MPC summary published that day has the same derived id and was therefore unfetchable. Found and recovered 2026-09-20 — it is a 21,715-character policy document. The scheduled-meeting veto does not cover this case: a summary published a week after the decision is not a calendar date. 15 CBRT and 10 BOI ids from the same era remain blocked; none of them currently collides with what the fixed scrapers find, but the mechanism is untouched. | `data/blocked_ids.json`, run 2026-09-20 |
| ~~B12~~ | ~~P1~~ | ~~**CBRT discovers ordinary press releases as policy statements.**~~ — **fixed**. Ten in two months for a bank that meets eight times a year, fetched and sent to the LLM classifier daily. See *Fixed this cycle*. | `update.py --dry-run --cb cbrt`, 2026-09-20 |
| ~~B13~~ | ~~P1~~ | ~~**`build_site.py` cannot run on Windows.**~~ — **fixed**; it died on the first line of its own `main()`. See *Fixed this cycle*. | run 2026-09-20 |
| ~~B14~~ | ~~P3~~ | ~~**`bcr.py` leaks a browser on every run.**~~ — **fixed, and the original description was wrong in a way worth correcting.** It is not every run and not only BCR: discovery does close its browser, but the first `fetch_text` reopens it and nothing closed it again, so the instance survived on `__del__` across all 8 Playwright-driven scrapers. `__del__` then runs at interpreter shutdown, when the driver may already be gone and its sync wrapper returns an un-awaited coroutine instead of closing — which the surrounding `except Exception` cannot catch, because a warning is not an exception. Cleanup now lives in `fetch_cb`'s `finally`, so it covers every exit path including a failed discovery. Original note: **`bcr.py` leaks a browser on every run.** `close()` calls `self._browser.close()`, which emits `RuntimeWarning: coroutine 'Browser.close' was never awaited` — the call returns a coroutine that is never awaited, and the surrounding `except Exception: pass` cannot catch a warning, so the browser is never actually closed. Seen on every BCR invocation on 2026-09-20. | `bcr.py:249` |
| ~~B15~~ | ~~P2~~ | ~~**Three CBs have no partial-failure detection.**~~ — **fixed 2026-09-22.** Below the collapse floor, discovery is now compared against the meetings the calendar places in the fetch window, which also removes the mirror-image false positive: a genuinely quiet window no longer reads as a broken scraper. A CB with no confirmed dates (BCR) has no expectation available and is left exactly where it was. Original note: The collapse heuristic needs a baseline of ≥4 (`_MIN_BASELINE_FOR_COLLAPSE`), but 18 of 23 scrapers filter discovery by `since_date`, so they report the 60-day window rather than the back-catalogue. Measured 2026-09-20: NORGESBANK 1, ECB 2, BCR 1–2, SARB 4, RBA 5, BOT 5. For those the zero check is the only discovery-side signal, and zero is not unambiguous either — a genuinely quiet 60-day window produces it (ECB's summer gap is ~56 days). The calendar probe and missing-meeting check are the real backstop, which is why D6 matters more than it looks. `README.md` and `SCRAPERS.md` both stated the back-catalogue property as though it were universal; SCRAPERS.md is corrected. | `logs/last_run.json` baselines, sweep 2026-09-20 |

## Data quality

| ID | Sev | Item |
|---|---|---|
| ~~D1~~ | ~~P1~~ | ~~**No version dimension**~~ — **code fixed**; version fields are now part of the write key so a new model build appends rather than overwrites. ⚠️ **`scripts/migrate_add_versioning.py` must be run once on `main` after merge** — it was deliberately not run in-branch, because the daily job rewrites those 39 MB parquets and a binary merge conflict would be unrecoverable. The code tolerates unmigrated data, so ordering is safe either way. |
| ~~D2~~ | ~~P2~~ | ~~**Three divergent "ensemble" definitions.**~~ — **unified 2026-09-22** on the maintainer's decision. `published_ensemble()` in parquet_store is the single accessor and everything reads it; the sentence-level mean is the definition kept, because averaging per-model doc_scores weights each *model* equally and silently mixes doc_scores computed over different sentence sets. Only two definitions were actually live — build_site's turned out to be an identity over a one-row-per-statement table. Original note:: sentence-level mean in `parquet_store.compute_ensemble_scores` (the published series); mean-of-doc-scores in `build_methodology_data.py:126`; a third per-sentence recomputation in `build_site.py:1005-1009` that also drops sentences under 10 chars and so can disagree with `doc_score`. Documented in `METHODOLOGY.md`, deliberately unreconciled — **fixing it moves published numbers, so it needs a maintainer decision.** |
| ~~D3~~ | ~~P2~~ | ~~**BOK 2026-06-28 is a Sunday** marked `confirmed: true`.~~ — **verified gone 2026-09-22.** No CB has a weekend entry anywhere in `forward_dates.json` (232 dates checked). Original note: The gap check now ignores weekend entries, but BOK is `_MANUAL_REFRESH` only so it will never self-correct — the bad row should be deleted. |
| ~~D4~~ | ~~P2~~ | ~~**BCR's calendar is entirely estimated.**~~ — **moot: BCR discontinued 2026-09-22** (I7). Original note: **Confirmed unfixable from here, 2026-09-21.** The page the item points at, `banrep.gov.co/es/calendario-junta-directiva`, is reachable without a CAPTCHA but is JS-rendered and carries no board-meeting data in its markup — the dates extractable from it are unrelated news items. Every other banrep path returns the Radware CAPTCHA, which is I7. So the confirmed dates cannot be transcribed by any means available here, and this waits on I7. Original note: **BCR calendar is 0 of 7 confirmed** — every date an estimate — while its WAF bypass probe depends on those dates, and the missing-meeting check cannot run for it at all. Fix: transcribe from <https://www.banrep.gov.co/es/calendario-junta-directiva>. |
| ~~D5~~ | ~~P3~~ | ~~**Calendar horizon ends 2026-12-23.**~~ — **closed 2026-09-20.** The live `refresh_calendars.py` run stored 46 dates and the horizon is now **2028-12-07** (FED 10, ECB 16, BOE 8, RBA 8, CBRT 4), each verified against the live page before saving because `_merge_dates` never deletes. What remains is **D6**: the 14 `_MANUAL_REFRESH` banks extend only by hand, and CBC, RIKSBANK and SNB are already down to one future meeting. |
| D6 | P3 | **13 of 24 CBs are manual calendar refresh** (`_MANUAL_REFRESH`); 11 auto-refresh. Riksbank moved to a fetcher on 2026-09-21. The remaining 13 extend only when a human transcribes them, which is why D14 exists. |
| ~~D9~~ | ~~**P1**~~ | ~~**Six of the 10 auto-refresh calendar fetchers parse zero dates, and BOE parses one.**~~ — **six of the seven fixed** (FED, ECB, CBRT, RBA, BOE, BOT), each with a parser tested against the captured page; see *Fixed this cycle*. **BOI was misfiled here**: its page is a Radware WAF challenge, not a parse failure, so it moves to **I9**. | `refresh_calendars.py --dry-run`, 2026-08-22 |
| ~~D10~~ | ~~P2~~ | ~~**`refresh_calendars.py` cannot fail.**~~ — **fixed**; see *Fixed this cycle*. Non-zero exit plus a per-fetcher status summary, with an expected-blocked list so known WAF failures do not cry wolf monthly. |
| ~~D8~~ | ~~P2~~ | ~~**NBP's 2026-06-10 calendar entry is wrong.**~~ — **verified gone 2026-09-22**: NBP's only June 2026 entry is 2026-06-02, confirmed, which is the date its own listing gives and the date in the store. Original note: NBP's own listing says that meeting was "held on 1-2 June 2026", and the backfill stored 2026-06-02. Every *other* 2026 NBP date matches the calendar exactly, so this is a bad row, not a scraper artifact. It now shows as an 8-day `date_mismatch`. **Do not just edit `forward_dates.json`** — `_merge_dates` is append/upgrade-only and the next monthly refresh re-adds it (trap 3.3); fix it in the fetcher. Left for a maintainer decision. |
<!-- D7 superseded by the row above, kept out of the table -->

| D11 | P2 | **The missing-meeting check ignores estimated dates, which is all the manual-refresh banks have.** `compute_calendar_staleness` only considers `confirmed` entries — correctly, since an estimate for a meeting that never happened would alert forever. The unstated cost was that the 14 `_MANUAL_REFRESH` CBs had no calendar-anchored protection at all and fell back to the interval clock at 1.5× the meeting gap (136 days for SNB). **Mitigated** 2026-09-20 by `compute_suspected_missing`, which covers estimated dates but only reports while the CB is already overdue on its own interval, so a wrong estimate self-clears. Still open as a data-quality item because the underlying calendars remain estimates: the fix improves detection, it does not make the dates right. |
| D12 | P3 | **A legitimate narrowing of discovery reads as a collapse forever.** The baseline only ratchets upward, deliberately, so that a broken run cannot redefine "normal" downwards — but that means an *intended* drop can never be forgotten. Fixing B12 moved CBRT from 37 to 12, permanently under its own collapse floor. **Mitigated** by `update.py --reset-baseline CB`, which is an explicit operator action and never automatic. Noted here so the next person who narrows a scraper knows the flag exists. |

| ~~D13~~ | ~~P2~~ | ~~**BCCh has three duplicated meetings in the 2026 store.**~~ — **removed 2026-09-22** on the maintainer's decision. The provisional row went and the calendar-confirmed one stayed in each case; the migration refuses to act if a survivor is not a confirmed calendar date. Verified afterwards by a real BCCh run: 2026-09-01 is corrected to 09-08, found already stored, and skipped. Original note: June, July and September 2026 are each present twice — 06-01/06-16, 07-01/07-28, 09-01/09-08 — because the slug discovery path synthesised a first-of-month date that nothing corrected, while the listing path stored the same meeting under its real date. The two carry slightly different scores, so the series counts those three meetings twice. **The cause is fixed** (the slug path now resolves its month against BCCh's calendar), so no new duplicates appear, but removing the three existing ones changes a published series and is a maintainer decision, like D2. Only 3 of 57 slug-family entries overlap; the rest are the historical record and must stay. |
| ~~D7~~ | ~~P3~~ | ~~**Six confirmed calendar dates disagree with stored meeting dates by 6-8 days.**~~ — **all cleared 2026-09-21**, and three were real data loss rather than hygiene: CBRT 2026-01-22 and 2026-03-12 and BCCH 2026-01-27 were missing statements, now fetched and scored. NORGESBANK 2026-06-11 was a wrong calendar row (2026-06-11-rate 404s, 2026-06-18-rate is the release). RBNZ 2026-04-08 was a wrong *stored* date from an uncorrected provisional. See *Fixed this cycle*. |

| D14 | P2 | **CBC and SNB cannot extend their calendars yet, and this is a wait rather than a bug.** Both are down to one future meeting and both publish forward dates only at a point in the year. CBC's source is a press release — "Provisional Schedule of Monetary Policy Meetings for 2026", released 2025-12-18 — listing March 19, June 18, September 17, December 17, all four already stored; the 2027 release is due around December 2026. SNB's decisions page lists only assessments that have happened and states the March/June/September/December cadence without dates. Checked 2026-09-21. RIKSBANK, the third CB in that warning, now has a fetcher and is clear. |

## Infrastructure & process

| ID | Sev | Item |
|---|---|---|
| ~~I1~~ | ~~P3~~ | ~~**requirements.txt is all floors.**~~ — **closed 2026-09-22.** Every requirement now stops below the next major above what is installed; pdfminer.six is left uncapped deliberately, being date-versioned. The lock was recompiled and diffed pin by pin afterwards: nothing added, nothing removed, no version moved. Original note: **Downgraded from P1 2026-09-22: the lockfile exists and is what every environment installs from.** `requirements.lock` is pinned and regenerated with an explicit `--python-version 3.11` (I10). What remains is that `requirements.txt` still carries 14 unbounded floors, which only matters at the moment someone regenerates the lock — the daily job no longer re-resolves anything. Original note: **`requirements.txt` is all floors with no lockfile**, re-resolved against PyPI latest on every daily run. **No longer hypothetical — this reached production:** `anthropic>=0.40.0` resolved to 1.0.0, which removed `temperature` from `messages.create()`, and every Claude call had been raising `TypeError` (surfaced as I3). `anthropic` is now pinned `>=1,<2`, but **every other dependency is still an unbounded floor** — `google-genai`, `openai`, `pandas`, `pyarrow` and `playwright` can each do the same thing tomorrow. A lockfile is the real fix. Raised to P1: it has demonstrated it can silently disable a scorer in production. |
| ~~I2~~ | ~~P2~~ | **Fully fixed 2026-09-21** — see I12 for the second half (issues now close themselves by reconciling against current state; 8 open became 2). The remaining concern, that the dedupe state lives in a git-committed file, no longer bites: a revert would be corrected on the next run rather than re-opening closed issues. Original note: **Half fixed 2026-09-20.** Alerts now retire when their condition clears — `new_active` only ever grew, so every id except `MODEL_FAILURE_` and `CALENDAR_COVERAGE_LOW` survived forever once raised, which both accumulated alerts for healthy CBs and (since the id is the dedupe key) stopped a recurrence after a fix from ever opening a fresh issue. The live file went from six entries to two. **Still open:** the state itself lives in a git-committed file. Original note: **Alert dedupe state lives in a git-committed file** (`active_alerts` in `logs/last_run.json`), so a revert re-opens closed issues. Live consequence: **BCR is open three times (#8, #11, #13)**, and **#5 CBRT / #6 BOI / #7 BCCH** are for CBs that are current today. |
| ~~I3~~ | ~~P2~~ | ~~**Claude scorer errored on the 2026-08-21 run**~~ — **fixed**; it was not flaky, it was total. Root cause was I1. See *Fixed this cycle*. |
| I10 | P2 | **Regenerating `requirements.lock` without `--python-version 3.11` breaks every environment.** Dropping scikit-learn (B9) removed its upper bound on numpy; the universal resolve then collapsed the marker pair `numpy==2.4.6 ; python_full_version < '3.12'` / `numpy==2.5.2 ; python_full_version >= '3.12'` into a single unconditional `numpy==2.5.2`, which needs Python 3.12. CI, the daily job and the local venv all run 3.11, so **CI runs 29–32 were red on "Install dependencies"** while the suite stayed green locally. Fixed, and the command is now pinned in both workflows, HANDOFF, PHONE_HANDOFF and the contract test's error message, with a test asserting the lock header still carries the flag. **Worth remembering for its own sake:** the pin-by-pin diff used to check that recompile reported "no version changed" because it compared versions and ignored markers — two entries collapsing into one looked like no change at all. |
| ~~I11~~ | ~~P1~~ | ~~**A concurrent push throws away the whole daily run.**~~ — **fixed 2026-09-21.** Run 150 fetched, scored, recomputed the ensemble and rebuilt both the site and the methodology page, then died on `! [rejected] main -> main (fetch first)` because a commit landed during the ~10 minutes between its checkout and its push. The deploy step depends on that one, so the public site was left stale too. The commit step now rebases onto whatever arrived and retries three times; everything it stages is generated output that nothing else writes. A genuine conflict still stops the run, because that would mean two writers of the same generated file. |
| ~~I12~~ | ~~P2~~ | ~~**`alert.py` could open issues but never close them.**~~ — **fixed 2026-09-21.** Eight auto-issues were open and six were for conditions already fixed. Yesterday's retirement fix could not help, because closing on the transition has nothing to fire on once an alert has already been retired — all six had been. It now reconciles against current state: every open `[AUTO]` issue whose alert id is not currently active is closed, whenever it cleared. New issues embed their alert id; older ones are matched on the stable part of their title, and missing-meeting titles include their date so clearing one does not close another. Verified on the live repo: 8 open → 2, and the 2 are BCR staleness and low calendar coverage, both real. |
| ~~I13~~ | ~~P3~~ | ~~**NBP's scraper-failure alert will reopen every three days.**~~ — **fixed 2026-09-22** with `EXPECTED_BLOCKED_SCRAPERS` in config. NBP is still recorded as failing and still printed in the run log; it no longer opens an issue. BCR is deliberately not in the set — its open issue is the standing reminder that I7 needs a decision. Original note: NBP cannot discover anything from a hosted runner — headless Chromium gets the Incapsula challenge — so `SCRAPER_FAILURE_NBP` is a true statement that nobody can act on until R4. The monthly calendar refresh already has `EXPECTED_BLOCKED` for exactly this, so the daily scraper alert wants the same concept: a known-blocked set whose failures are reported but do not raise an issue. Left open rather than suppressed, because silently hiding a real failure is the worse error. |

| ~~I4~~ | ~~P3~~ | ~~**75** pre-existing `ruff` findings.~~ — **cleared 2026-09-21, and the CI lint step is now blocking** rather than continue-on-error. Three were worth fixing for their own sake: three `zip()` calls that would have silently dropped a chart panel on a length mismatch now pass `strict=True`. The 47 remaining E402s are structural (scripts/ puts the repo root on sys.path before importing) and are a documented per-file ignore. Original note: **75** pre-existing `ruff` findings (was ~79); the CI lint step is `continue-on-error` until that backlog clears. Many are mechanical `B904`. |
| ~~I5~~ | ~~P3~~ | ~~**BOJ is excluded** from the pipeline but retains a scraper module and calendar entries.~~ — **resolved 2026-09-22 as far as it should be.** The monthly refresh was telling the operator to transcribe BOJ dates every run, which is work that cannot have an effect; BOJ is now marked excluded and reported as such. The scraper module and calendar entries are deliberately **kept**: re-enabling BOJ should not mean rewriting its scraper, and the gap checks already filter on the CBs the pipeline runs. A test asserts the excluded set and `CB_ORDER` cannot drift apart. |
| ~~I7~~ | ~~**P1**~~ | ~~**BCR is hard-blocked by ShieldSquare/Radware.**~~ — **decided 2026-09-22: BCR is discontinued.** The maintainer's call, and the right one — the series could not be kept current and a stale series on a live dashboard is worse than none. Out of the pipeline, the dashboard, the groups and the staleness clock; its 204 statements (2007 to 2026-07) stay in the store for research, and it appears on the methodology page's excluded table with its reason. Original note: `banrep.gov.co` returns an 875-byte Spanish CAPTCHA page citing *network reputation* ("comportamiento anómalo malicioso detectado previamente en su red") with an Incident ID. Measured 2026-08-22 from the maintainer's residential IP with headed Chromium **and** playwright-stealth — the combination that defeats NBP's Incapsula — and it still fails. So this is **not** fixed by R4's self-hosted runner. Options are a CAPTCHA-solving service, a different source for Colombian policy decisions, requesting an allowlist from Banco de la República, or accepting BCR as permanently manual. **Needs a maintainer decision.** BCR is the only stale CB left. |
| ~~I8~~ | ~~P3~~ | ~~**The repo has no lockfile and CI/prod/local resolve independently.**~~ — **fixed.** All three install from `requirements.lock`; verified 2026-09-21 that the local venv matches it exactly. Original note: Related to I1 but distinct: the local venv built today resolved `anthropic` to 1.0.0 while the last green CI run had resolved an older 0.x, so CI was green on a tree production no longer had. |
| ~~I9~~ | ~~P2~~ | ~~**BOI's calendar page is behind a Radware anti-bot interstitial.**~~ — **fixed 2026-09-22 by R4.** The monthly refresh now has a second job on the self-hosted runner that collects NBP's and BOI's calendars with a real browser. Original note:, not a broken parser. Split out of D9 on 2026-08-23 after reading the captured page: HTTP 200, 118 KB, and **106 characters of visible text** reading *"Verifying your browser before proceeding… Incident ID: …"*. No parser change can fix it, so it belongs with the WAF-blocked group (NBP, BCR, BCCH) and wants the same treatment — residential IP plus headed Chromium (**R4**), which is the NBP recipe, not the BCR one (I7). `is_waf_challenge()` now detects this family (Radware, Incapsula, Cloudflare, ShieldSquare) and BOI reports it explicitly instead of disappearing into the generic "0 dates" warning. **Note the statement scraper for BOI is unaffected — BOI is current.** This is the calendar page only. |
| ~~I6~~ | ~~P1~~ | ~~**The CI test gate never ran a test.**~~ — **fixed**; see *Fixed this cycle*. Runs 2–13 were all red. |

## Outstanding work

> ### ✅ The six repaired calendar parsers are verified against the live pages
>
> Done 2026-09-20 on the maintainer's machine. `refresh_calendars.py --dry-run`
> against the real sites: **FED, ECB, BOE, RBA and CBRT all OK**, reading the
> decision column, not a neighbouring one. 46 dates were then written and the
> horizon moved 2026-12-23 → **2028-12-07** (D5 closed).
>
> Two differences from what the fixtures predicted, both benign:
>
> * **BOT and CNB returned fewer dates than the floor** and failed the run.
>   Neither is broken — they publish one year at a time and their 2027 pages do
>   not exist yet (CNB's 404s). The floor now only fails on a *regression*; see
>   *Fixed this cycle*.
> * **BOI still returns nothing**, exactly as I9 predicts, and is reported as
>   `BLOCKED` rather than as a parse failure.
>
> The dates written match the fixture predictions for every CB.

| ID | Item | Blocked on |
|---|---|---|
| ~~R10~~ | ~~**Decide what to do about the 25 stale blocked ids**~~ — **done 2026-09-21**, 21 ids remain and none is CBRT or BOI. | Nothing. They came from the over-inclusive discovery fixed in B12 and the old BOI pagination. None currently collides with what the fixed scrapers find, so this is cleanup, not an outage — but B11 is the mechanism by which one of them cost a real statement |
| ~~R11~~ | ~~**Give BOI's calendar page the NBP treatment** (I9)~~ — **done 2026-09-22**, as a second job on the monthly refresh. Original note: | R4, or a local headed run. Its *statement* scraper is fine; only the calendar fetcher is blocked, so BOI's forward dates never extend |
| ~~R3~~ | ~~**Backfill NBP**~~ — **done** (2026-06-02, 2026-07-08). The stated cause was wrong; see *Fixed this cycle*. Issue #14 can close. |
| ~~R4~~ | ~~Self-hosted runner for the WAF-exposed CBs~~ — **done 2026-09-22.** `premm-desktop` is registered and online, started at logon by a scheduled task rather than installed as a service: a Windows service runs in session 0 with no desktop, and NBP needs *headed* Chromium, so a service would have reproduced the hosted runner's failure exactly. `waf_update.yml` fetches NBP, BOI and BCCH daily at 14:00 UTC. Measured on the first green run: hosted discovers **0** NBP statements, this discovers **296**. Setup, and how to stand one up elsewhere, is in [`SELF_HOSTED_RUNNER.md`](SELF_HOSTED_RUNNER.md). Original note: | **Scope now measured, and it is smaller than assumed.** A residential IP alone fixes nothing: NBP needs `PLAYWRIGHT_HEADLESS=0` (headed Chromium **plus** stealth) and browser-context fetching, both of which now exist — so a runner helps NBP only if it has a real display. **BCR is not fixed by a runner at all** (see I7). BCCH and BOI are currently *current*, so the runner's value for them is untested. |
| R8 | Capture HTML/PDF fixtures per CB + parser regression tests | **In progress.** 8 calendar fixtures (2026-08-22) plus the first three statement-side ones on 2026-09-20: `rbnz_listing_wayback.html`, `boi_rate_announcements_2026.html`, `boi_rate_announcements_2013.html`. Remaining: a statement listing page per CB for the 11 still on a single path |
| R9 | Harden the single-path scrapers | **6 of 16 done.** FED (calendar probe), RBNZ (Wayback discovery), BOI (listing API), CBRT (precision), ECB (index fallback, 09-21), **NORGESBANK (calendar probe, 09-22)**. **10 left:** BANXICO, BCB, BOK, BOT, CBC, CNB, MNB, RBA, RBI, SARB — all now covered for *detection* by the collapse check or, below its floor, by the calendar comparison added in B15; what they still lack is a *recovery* path. BANXICO's three chained CSS-class regexes are the most brittle of the ten. Original note: **4 of 15 done** (2026-09-20): FED (calendar probe), RBNZ (Wayback discovery), BOI (listing API), CBRT (precision, not redundancy). **12 left:** BANXICO, BCB, BOK, BOT, CBC, CNB, **ECB**, MNB, NORGESBANK, RBA, RBI, SARB. Prioritise by B15 — NORGESBANK, ECB and BCR have no collapse detection, so a partial failure there is invisible |
| ~~—~~ | ~~**Run `scripts/migrate_add_versioning.py` on main after merge**~~ — **done** in commit `81d7f1b` (2026-08-22). Verified 2026-09-20 against the store: `model_version`/`prompt_version` present, 13,151 historical rows carry the `unversioned` sentinel, recent rows carry real model ids, and `ensemble_scores.parquet` exists. |
| — | **Run a real bake-off** | **Ready to run, 2026-09-22 — waiting on the maintainer, who has asked to run it later.** Everything below was checked or fixed today; the only step left is spending. **The cost is $13.62, not the $2–4 this row used to say** — measured by the harness itself against the committed sample: 1,600 statement-scores (200 × 4 models × 2 replicates), and gpt-5 is the expensive part. Run it with `python scripts/model_bakeoff.py score --models claude,gemini,deepseek,openai --replicates 2 --yes`, then `report --candidate openai`. `--replicates 2` is not optional: without it test–retest is unmeasured and the verdict refuses to pass the candidate. Original note: **Spend decision** — ~$2–4 for 200 statements × 4 models. The harness is built and exercised against a stub; it has never been run against a live API |
| ~~—~~ | ~~Backfill the 54 charset-corrupted FED statements~~ — **done 2026-09-22**, approved by the maintainer. 54 re-fetched, 162 scorings, zero errors; 53 published scores moved and nothing outside the set changed. Zero corrupted statements or sentence rows remain in the corpus. Original note: **Spend decision** — needs re-fetch + re-score of 54 × 3 models, which also trips `COST_GUARD_MAX = 20` |

---

## Fixed this cycle

Kept for evidence — several of these had been silently degrading the data for
months, and the record is the point.

| Item | Why it mattered |
|---|---|
| **Getting the bake-off ready to run (B1-adjacent, I14)** | Asked what stood between here and a bake-off, and three things did. **The crash the backlog predicted was real and still there:** `model_bakeoff.py` prints its scorecard to stdout, the scorecard contains a `≥`, cp1252 cannot encode it, and `report` would have died on its own output *after* `score` had spent the budget. Reproduced, then fixed. The contract test written the day before should have caught it and did not — it looked only inside `print(...)` calls, and the scorecard is a list of f-strings printed by an all-ASCII `print("\n".join(out))`. Strengthened to ask "does this module print at all, and does it contain non-ASCII anywhere", which immediately found three more: `llm_calendar_check.py`, `staleness_matrix.py` and `update.py`, the daily job itself. **Second**, the candidate can silently stop being comparable: gpt-5 rejects `temperature`, the scorer drops it to keep the run alive, and test–retest — the one absolute metric here — then means something different for the candidate than for the pinned incumbents. Scorers now declare whether they are pinning, and the scorecard prints it beside test–retest with a warning naming any model that was not. **Third**, checked rather than assumed: all 200 sample ids still resolve after today's deletions, the production parquets are only ever read, and `score` fails fast on an unknown model name before spending anything. |
| **BOK reported a total outage as an empty listing, and NORGESBANK could not have (R9)** | Two halves of the same lesson on the same day. BOK timed out three times against bok.or.kr on a hosted run, aborted its scan and returned `[]` — recorded as `discovery_ok: True, errors: 0, discovered: 0` for a CB whose listing holds 100 decisions. It raises now, and only an *aborted* scan raises: a test caught the first version treating two timeouts followed by a good page as a failure. NORGESBANK is the mirror image — its listing is an opaque CMS facet API and it reports 1-2 refs in a 60-day window, so nothing could have distinguished a partial failure from a quiet fortnight. It now has a calendar probe over its fully date-derived URLs, a pattern verified on the live site while correcting D7. |
| **BCR discontinued (I7, B10, D4)** | Decided by the maintainer rather than fixed, and the right way round: re-verified from a residential IP with headed Chromium and stealth, banrep.gov.co still returns the ShieldSquare CAPTCHA, so the series could not be kept current — and a stale series on a live dashboard is worse than no series. Removed from `_SCRAPERS`, `CB_ORDER`, the LATAM and EM groups, the staleness clock and the calendar refresh; its two site JSON files are deleted. **Its 204 statements stay in the store**, 2007 to 2026-07, because they are useful for research, and CB_META keeps its label and colour so those rows still render for anything reading them directly. The scraper module stays too, and the contract test now treats it like BOJ — an excluded module must *not* be registered, which is what stops it being silently reactivated. On the methodology page it moves from the coverage table to the excluded table, with its reason and retained count; listing it as covered would claim a live series. |
| **The self-hosted runner (R4, R11, I9)** | Four things failed silently while standing it up, each costing a run: `pwsh` and `bash` are not on the runner's PATH; PowerShell 5.1 parses `&&` as an error; the runner sets `$ErrorActionPreference = Stop`, so git's progress output on stderr failed two steps that had *already pushed successfully*; and a job that pushes needs `permissions: contents: write` or the checkout token is read-only and the push 403s. The biggest trap was the one in the old RUNBOOK: installing the runner as a Windows service. A service runs in session 0 with no desktop, and NBP needs headed Chromium, so that would have produced a runner that fails in exactly the way the hosted one does. It runs interactively from a logon scheduled task instead. All of it is written down in SELF_HOSTED_RUNNER.md so the next machine does not rediscover it. |
| **The 54 charset-corrupted FED statements (B1)** | `requests` defaults text/* to ISO-8859-1 when the server declares no charset, and the Fed declares none, so every en-dash and curly quote in 54 statements between 2007 and 2026 was stored as mojibake — **and the models scored the corrupted text.** Re-fetched rather than repaired: `repair_mojibake()` exists but does not fully clean these documents, while a re-fetch returns what the Fed serves today through the path the daily job already uses. All 54 came back clean and 2–4 characters shorter, which is mojibake collapsing back to single characters. Re-scored across all three models, 162 scorings, zero errors. The published numbers moved — that is the point, they were produced from corrupted input — and the migration asserts the part that matters: no statement outside the backfill set changed by more than 1e-9. |
| **Window-scoped scrapers were judged by a yardstick that could not measure them (B15)** | 18 of 23 scrapers filter discovery by `since_date`, so what they report is the 60-day window, not a back-catalogue: NORGESBANK returns 1, ECB 2, SARB 4. The collapse test needs a baseline of at least 4, so for those CBs it never ran — a partial failure, two meetings due and one resolved, was indistinguishable from a healthy run. And the same confusion ran the other way: zero was treated as broken when a quiet window produces it, and ECB's summer gap is about eight weeks against a 60-day window, so the rule would eventually have raised an alert for a bank that simply had no meeting. Discovery below the floor is now compared against the confirmed meetings the calendar places inside the window — what the CB was *due* to publish, independent of how its discovery counts. Above the floor nothing changes, because FED returning zero is broken whatever the calendar says. |
| **A block refused a date, not a document (B11)** | Statement ids are derived from CB and meeting date, so every document published that day maps to one id — and `blocked_ids.json` is consulted before any scraper runs, with nothing ever expiring. It had already cost a statement: a CBRT liquidity-management press release of 2026-09-17 was auto-blocked, which made that day's genuine MPC summary, 21,715 characters of policy, unfetchable. The scheduled-meeting veto could not help, since a summary published a week after the decision is not a calendar date. Blocks now name the rejected document by URL. The 21 historical id-only entries keep their old meaning, because reinterpreting them would silently unblock genuine rejections — trap 3.4 in the other direction — and a test pins that every one of them still blocks outright. |
| **BOK reported a network outage as an empty listing (R9)** | The hosted run of 2026-09-21 recorded `discovery_ok: True, errors: 0, discovered: 0` for a CB whose listing holds 100 decisions. What happened was three consecutive 30-second timeouts against bok.or.kr, after which the scan aborted and returned `[]` — the value that means "quiet". It raises now, carrying the underlying error so the next reader is not sent looking at selectors. Only an *aborted* scan raises: a test caught the first version getting that wrong, since two timeouts followed by a good page is a working scraper. Worth noting how it surfaced — only the collapse check (100 → 0) caught it, and a CB with a baseline under 4 would have shown nothing at all, which is B15. |
| **BCCh was storing the same meeting twice, and had lost one entirely (D7, D13)** | Its slug discovery path knows only a month from the URL (`comunicado-rpm-enero-2026`) and synthesises the first of it. RBNZ does the same but corrects it from the page on fetch; BCCh cannot, because a comunicado states when its *minutes* will appear and when the *next* meeting is, never its own date. So the slug path and the listing path each stored the same meeting under a different date, and June, July and September 2026 are each in the store twice with slightly different scores. It also meant January 2026's decision was never stored at all — what looked like an 8-day `date_mismatch` was a missing statement. The slug path now resolves its month against BCCh's own calendar, which is fully confirmed and holds exactly one decision per month, and update.py's existing correction then either fixes the date or skips the ref as already stored. That recovered 2026-01-27. The three existing duplicates are left for a maintainer decision (**D13**) because removing them moves a published series. |
| **A calendar fetcher for Riksbank (D6)** | It was `_MANUAL_REFRESH`, so its forward calendar only moved when a human transcribed it, and nobody had: it was down to one future meeting and both remaining entries were estimates — *wrong* estimates, 2026-09-10 and 2026-11-19 against real decisions on 09-24 and 11-04. The discrimination is trap 3.7 in its purest form: the page lists three rows per decision, one day apart — the meeting, the publication of the decision, and the press conference — and all three match "monetary policy decision". The store holds the published decision (2026-03-19 against a meeting on 03-18), so the publication row is the one that matches; a test asserts the meeting dates are not produced. 7 confirmed dates written, to 2027-06-22. |
| **Cleanup moved out of each scraper and into fetch_cb (B14)** | The backlog said bcr.py leaks a browser on every run. Both halves were wrong and the real shape was broader: discovery does close its browser, the first `fetch_text` reopens it, and nothing closed it again — across all 8 Playwright-driven scrapers, in a process that works through 23 CBs. `__del__` cannot cover it, because at interpreter shutdown the driver may be gone and the sync wrapper then returns an un-awaited coroutine rather than closing, which `except Exception` cannot catch. Closing now happens in fetch_cb's `finally`, covering all four returns and the raise. **This also took CI red for three runs**: the first version of the tests drove update.py in a subprocess, and update.py hard-exits outside `.venv`, which CI does not use — the repo's own "nothing testable in scripts/" trap in a shape it had not taken before. The logic moved to `src/scraper_lifecycle.py`, and the wiring is now checked with two AST assertions instead, one of which fails if a `return` is ever added outside the try. |
| **The five calendar date mismatches were three missing statements (D7)** | Filed as calendar hygiene since August. Three were not: CBRT 2026-01-22 and 2026-03-12 (the store had each meeting's *summary* but not its decision) and BCCH 2026-01-27 were missing statements, all now fetched and scored. NORGESBANK 2026-06-11 was a bad calendar row — `2026-06-11-rate` 404s while `2026-06-18-rate` is the release — and RBNZ 2026-04-08 was a bad *stored* date, a provisional YYYY-MM-01 that never got corrected, confirmed by re-fetching the page. |
| **25 stale blocked ids cleared (R10)** | Left by the junk discovery in CBRT and BOI. Inert once those scrapers were fixed, but they kept B11 armed: statement ids are date-derived, so a block on a junk document silently blocks any real document sharing its date, which had already cost the CBRT summary of 2026-09-17. Checked before removing that none is stored, none is discovered by the fixed scrapers, and none of the dates is a real decision date for its CB. |
| **CBRT was discovering press releases as policy statements (B12)** | Ten documents in two months, for a bank that meets eight times a year: balance-of-payments statistics, FAST transaction limits, an issue of TRY 50 banknotes, an OIC-COMCEC forum, the results of a paper contest for university students. Each was fetched and sent to the LLM classifier **every day**, and that classifier — check 5 of `validate_new_statement` — was the only thing standing between a university paper contest and a dataset of monetary policy decisions. The ANO fallback admits a press release whose title did not identify it as an MPC document, on the strength of a content check, and two paths skipped that check: a PDF short-circuits the entire verification block (`is_pdf`), and most of these are served as PDFs, so the gate never ran; and a failed detail fetch logged a warning and **fell through without `continue`**, admitting the candidate unverified. A PDF still cannot be content-verified without downloading ~40 documents a year, so it is verified by the title on the listing *card* — PDF links have no anchor text of their own, which is precisely why the title filter missed them. Live check: 2026 discovery 37 → 12, all 9 stored 2026 statements still found, none of the 10 press releases. **It had already cost a real statement** — see B11. |
| **RBNZ discovery had no fallback, and its content fallback was unreachable (B5)** | RBNZ failed on 2026-09-18 and 09-19 — discovery 0 against a baseline of 154 — while the identical call succeeded by hand. A transient Playwright timeout, and two defects turned it into an outage. `discover_urls` had no fallback at all, so one `goto` timeout took out all 154 refs, where the Wayback fallback existed only for *content*, the path where a failure costs one statement. And B5 itself: that fallback caught only `RuntimeError`, so a `TimeoutError` — the most likely failure — walked straight past it, meaning the fallback had been unreachable for its own main case since it was written. Discovery now falls back to the archived listing (verified live: 154 refs, identical to the live page), both paths catch `PlaywrightError`, and both failing raises rather than returning `[]`. The CDX lookup was a single 15s shot that timed out on the first real call this fallback ever made, and again at 20s — a fallback that needs a retry to work does not work, so it retries at 20s and 45s. |
| **BOI's discovery collapsed intermittently, and was filed as a WAF problem (I9-adjacent)** | Discovery fell from 131 to 1 for three days, then recovered by itself on 2026-09-20 — which is worse than a clean failure, because intermittency is what defeats a three-consecutive-runs detector. It was not the WAF: the page loads and states "Interest Rate Announcements 145" in its own filter. Two UI interactions had broken. Applying the filter *navigates*, destroying the execution context the click was made in; and the pager silently **drops the filter**, so page 2 of a filtered list is page 2 of the unfiltered press releases, where rate decisions are sparse — the keyword guard then correctly rejected nearly everything, and the collapse looked like a quiet month. The pager also only renders on some loads. Discovery now posts to `/BoiControllers/PressReleases/Filter`, the controller the site's own UI uses, one year at a time: ~8 decisions a year sits under the 13-item response cap, so there is no pagination to get wrong and no UI state to lose. The filter id and the expected total are read from the page rather than hardcoded. Three things the work turned up: there is no working page parameter (ten spellings tried), so a year over the cap silently truncates and is now re-queried in halves; the page reloads itself under the WAF challenge, so any wait races the navigation — one run had all 15 year queries return the whole page instead of the fragment and the next had all 15 work, so the session is now *proved* usable with a probe query before the real ones are issued; and `wait_for_selector` defaults to `state="visible"`, which a hidden antiforgery-token input can never satisfy, so that guard could never have fired. Also: the keyword guard admitted "Dates of interest rate decisions in 2017", a schedule announcement — only its age kept it out of the store. |
| **FED's three discovery strategies were one (B6)** | `skip_historical` is True whenever `since_date` is 2021 or later, which is every daily run, so strategies 1 and 3 never executed and what remained was a single page inside a `try/except` that logged a warning and returned `[]`. A redesign of that page would have produced "0 discovered, no error" on the most-watched central bank in the set. The fix is a real second path rather than the appearance of one: FOMC statement URLs are fully determined by the meeting date (`monetary{YYYYMMDD}a.htm` for every statement in the store) and the calendar now carries confirmed FOMC dates through 2027, so `probe_from_calendar` covers exactly what the listing page cannot — it never reads that page. It probes the meeting date *and* the day after, because an FOMC meeting runs two days and the statement carries the second while older calendar entries record the first (the store has September 2026 at 09-16, the calendar says 09-15, and `_merge_dates` cannot correct it). |
| **`build_site.py` could not run on Windows at all (B13)** | It died on the first line of its own `main()`, on a box-drawing character, so the dashboard could not be rebuilt locally — on the machine HANDOFF directs the operator to, in the step a local backfill needs to publish what it just fetched. Same bug class as the Fed charset bug and the `read_text` sweep before it, in the one place those fixes did not reach: they covered *file* I/O, and this is stdout, which Python encodes with the platform's preferred encoding (cp1252 here whenever output is redirected). What kept it hidden is that cp1252 happens to contain the em-dashes and middots these scripts mostly print, so four of the five affected scripts run fine and only a character outside the codepage trips it — the guard therefore belongs on the script, not the character. `src/console.py` forces UTF-8 with `backslashreplace`, and a contract test parses every script in `scripts/`, finds those printing a non-ASCII literal, and fails if one does not call it. `logging` was never affected; its StreamHandler already backslash-escapes rather than raising, which is why the daily job survived this. |
| **Estimated calendar dates had no gap check (D11)** | The missing-meeting check considers only `confirmed` dates, for a good reason — an estimate for a meeting that never happened would alert forever and nobody could clear it — but the cost was never written down: the 14 `_MANUAL_REFRESH` banks have calendars that are almost entirely estimates, so they had no calendar-anchored protection at all and fell back to the interval clock at 1.5× the meeting gap, which for SNB is 136 days. `compute_suspected_missing` covers estimated dates without the forever-alert problem by requiring corroboration instead of ignoring them: a miss is reported only while the CB is already overdue on its own typical interval, so a wrong estimate stops being reported the moment the CB publishes. Against the live store it reports two, and the pair shows why the tier is "suspected" rather than "missing" — SNB 2026-09-17 is a *wrong estimate* (checked directly: their decisions page lists only March and June for 2026, so the Q3 assessment has not happened yet), while BCR 2026-08-28 is the real I7 outage. |
| **The calendar floor failed every autumn (D10 follow-on)** | The live verification of the D9 parsers passed — FED, ECB, BOE, RBA and CBRT all read the real pages — but the run exited 1 on BOT and CNB, and both were false alarms. Most of these banks publish one calendar year at a time, so BOT and CNB legitimately have two 2026 meetings left and no 2027 page yet (CNB's 404s), giving 2 dates against a floor of 3. A monthly job that goes red every autumn for a known reason is one the maintainer learns to skim, which is the exact blindness D10 exists to remove. The floor now only fails when the result is also a *regression* — fewer dates than the calendar already stores for the future — which still catches the bug that motivated it (BOE returned 1 against 3 stored). Also, every status line was logged at ERROR whenever any fetcher failed, so a healthy fetcher printed as `ERROR  OK  BOE`. |
| **BCR's probe reported finds it had not made** | It announced `found BCR_RATE_DECISION_20260828` every run; that URL returns a "Página no encontrada" page as HTTP 200, and the LLM classifier threw it out — a fetch and a classification a day to rediscover that nothing is there. The validator asked whether "banrep" appeared in the body, and since the probe runs on plain `requests` (deliberately: ShieldSquare blocks Playwright from CI but serves ordinary GETs), no JavaScript runs and *every* URL returns the same ~16 KB shell, so that check was true of the soft 404, the CAPTCHA and a real decision alike. It now rejects both failure pages and requires the page to mention a policy rate. It also probes the URL shape BanRep moved to partway through 2026 (`/noticias/junta-directiva/`, nested), since April and June are stored flat and July nested. **This does not recover BCR** — checked rather than assumed: the nested August URL is a genuine 404 and July and September return the ShieldSquare CAPTCHA from a residential IP with headed Chromium and stealth. I7 stands. What changes is honesty: BCR reports a MISS, so its real state is visible. |
| **D5 closed: the calendar horizon reaches 2028** | The first live run of the D9 parsers, and the commit where that work actually took effect. 46 dates written, each verified against the live page before saving because `_merge_dates` never deletes and trap 3.7 would have made a wrong column permanent: FED 10 (to 2027-12-08), ECB 16 (to 2028-12-07, it publishes two years ahead), BOE 8 (stored unconfirmed — the page says provisional), RBA 8, CBRT 4 (its page runs to mid-2027 only). Horizon 2026-12-23 → **2028-12-07**. |
| **The calendar refresh could not fail (D10)** | The job that was supposed to keep the calendar current had no way to report that it had stopped. Every fetcher swallows its own exception and returns `[]`, an empty result logs at WARNING, and the run exits 0 — so a refresh that had silently died looked identical to one where nothing changed, which is exactly how D9 survived an unknown number of monthly runs. `check_calendar_health.py` did not cover it either: it validates the stored *file*, and duly reported "All active CBs have adequate forward coverage" while six fetchers returned nothing. Now each fetcher's outcome is classified (`ok`/`empty`/`error`/`thin`/`blocked`) in `src/pipeline_health.py`, the script exits non-zero on any real failure, and the workflow prints a worst-first summary. Two design points earn their keep: **`thin`** catches the partial parse a zero-check misses — BOE's single date for an eight-meeting bank looked like success to every check in place — and **`EXPECTED_BLOCKED`** (NBP, BOI) keeps known WAF blocks from turning the job red every single month, because an alarm that always fires is the same blindness wearing a different hat. Removing a CB from that set is the switch that makes its failures count again |
| **Six dead calendar parsers (D9)** | FED, ECB, CBRT, RBA and BOT were returning zero dates from healthy pages; BOE returned one for a bank that meets eight times a year. Each page had simply changed shape, and every fetcher swallows its exception and returns `[]`, so the monthly job reported "no changes" and exited 0 throughout. Each is now a `parse_*_calendar(html, today)` split out from its fetch and tested against the real captured page. Causes, all different: **FED** moved to `.fomc-meeting` classes; **ECB** puts the date and its description in sibling `<dt>`/`<dd>` nodes, which the old code neither iterated nor could match, since it required both in one tag; **CBRT** writes "January 22, 2026" while the regex wanted "22 January 2026"; **RBA** and **BOE** never put the year in the row at all (it is in the table caption / `<h2>`); **BOT** ships its table HTML-escaped inside a JSON attribute, so the rendered page has 14 KB of navigation chrome and no dates in 170 KB of markup. **The bigger risk was the naive fix.** Five of these six tables sit beside minutes, report or second-board columns whose dates are indistinguishable to a regex — a scan-every-cell repair would have written ~14 CBRT publication dates, up to 8 RBA Payments System Board dates and 7 BOT report dates into the calendar as if they were rate decisions, and the missing-meeting check would have fired on every one. Each parser selects its decision column by header name, and each has a test asserting the neighbouring dates are *not* produced. Two further traps are pinned by tests: a FED asterisk means a Summary of Economic Projections, **not** a cancellation (the old docstring had it backwards — skipping them drops half the year), and the ECB page contains a Day-1 entry with no "(Day 1)" label, so the parser takes the last day of each consecutive run rather than trusting the labels |
| **SNB's 156-day gap was a false-positive garbage filter, not the blocklist (R-SNB)** | The handoff diagnosed SNB as purely a wrongly-blocked ID and expected section 2.4 to be a confirmation step. Unblocking was necessary but not sufficient: with the block lifted the scraper fetched the correct 4,941-character press release and `validate_new_statement` check 4 discarded it again on the same run. `_GARBAGE_PERMANENT` matched `"Download file now
Monetary policy"` **anywhere** in the document — but a genuine SNB assessment contains that exact pair thousands of characters in, as the caption of the conditional inflation forecast chart followed by the assessment title. So every valid SNB release was rejected, and because the pattern is *permanent*-class it also made them auto-blockable — which is how 2026-06-18 came to be blocked at all. The blocklist was the symptom; this was the cause. Now anchored to the first 200 characters, with `tests/test_garbage_patterns.py` pinning both directions. SNB is current. |
| **The Claude scorer had been failing on every call (I3 → I1)** | I3 recorded this as "errored once, counter 1 of 3, below threshold, watch it" — a flaky-looking blip. It was neither flaky nor minor: `anthropic` 1.0.0 removed `temperature`/`top_p`/`top_k` from `messages.create()`, `requirements.txt` asked only for `>=0.40.0`, and every Claude call had been raising `TypeError` since the SDK bumped. The alert threshold hid it because the counter only reaches 3 on consecutive days a Claude-eligible statement exists, and the pipeline had almost nothing new to score while three CBs were stale — **two failures masking each other**. The fix is `extra_body={"temperature": 0}`, not deletion: `temperature` is gone from the 1.x signature but not from the API, and `claude-sonnet-4-6` still honours it. Deleting it would have silently fallen back to the default temperature, making every score non-reproducible and quietly invalidating the test–retest metric in `src/model_eval.py` — whose entire purpose is to check a model agrees with itself at temperature 0. `anthropic` is now pinned `>=1,<2`. |
| **NBP was not an IP problem (R3, issue #14)** | The handoff's stated cause — Incapsula blocks GitHub's datacenter IPs, so a residential IP walks straight through — is wrong, and acting on it alone would have produced another "fixed" scraper that still returns nothing. Measured from the maintainer's residential IP: plain `requests` **and** headless+stealth Playwright both get the 884-byte Incapsula challenge with zero links. Only headed Chromium **plus** playwright-stealth returns the real 283 KB listing — each half alone fails. Discovery alone was still not enough: every PDF then 403'd, because the Incapsula cookie is bound to the browser's TLS/JS fingerprint and does not survive being handed to `requests`. NBP now falls back to fetching through the browser context's own request API. 295 statements discovered, both missing meetings stored, NBP current after 108 days. |
| **Every text file I/O in the repo assumed the platform default encoding** | Python's text I/O follows the locale: UTF-8 on the Linux CI runner, cp1252 on the maintainer's Windows machine. So 31 bare `read_text()` calls plus a dozen `write_text`/`open` calls worked in CI and crashed locally — on the very machine the handoff directs the operator to. `staleness_matrix.py`, the section 2.4 recovery check, exited 1 on any scraper source containing an em-dash and could not run at all. `model_bakeoff.py` writes a scorecard containing `≥` and `·`, so a real bake-off would have crashed **after** spending the API budget. The suite reported 3 failed / 319 passed locally against 322 passed in CI. Same bug class as the Fed charset bug (B1) — an undeclared encoding — this time in the tooling instead of the scrapers. |
| **The scheduled-meeting auto-block veto was confirmed working under real conditions** | Not a fix — evidence. Both SNB and BCR fetched rejected content this cycle and both logged "is a SCHEDULED meeting but content was rejected … not blocking; investigate the scraper" instead of being silently re-blocked. Under the old behaviour this session would have re-blocked both IDs and re-created the exact outage it was sent to repair. |
| **`data/scored/*.parquet.lock` were tracked in git** | `filelock` artifacts committed by the daily runs on 2026-07-03 and 2026-07-07 and never covered by `.gitignore`. Running the test suite deletes them, so every working tree showed spurious deletions — noise directly in front of trap 3.1, which asks you to read `git status --porcelain data/` carefully before every commit. |
| **Bake-off harness (C3c)** | `scripts/model_bakeoff.py` + `src/model_eval.py`: a standing framework for deciding whether a new model release is worth adopting, on a fixed stratified 200-statement sample that is committed so successive bake-offs stay comparable. Adds **test–retest** — the same model on the same sample twice — which had never been measured, and is the only absolute criterion here: a model that disagrees with itself is unusable however well it correlates with the incumbents. Writing the tests found three bugs in my own metrics: `pairwise_matrix` returned a column-less frame that made `verdict` raise instead of reporting "no comparison available"; the **bias sign was inverted** whenever the candidate sorted second, which would have described a dovish model as hawkish and flipped the recommendation; and `coverage`/`null_agreement` came off a `dropna(how="all")` pivot that discarded exactly the rows where both models agreed a sentence was boilerplate |
| **The CI gate never ran a test (I6)** | The "first CI gate in this repo" was added with a hand-picked dependency list — `pytest pandas pyarrow python-dotenv ruff` — that did not cover `requests`, `filelock`, `pdfplumber` or the three model SDKs. Five test modules failed at *collection*, so **runs 2 through 13 were all red and zero tests executed**. The suite was green locally the whole time, which is exactly why it went unnoticed: local reporting was true, and never checked against CI. It now installs `requirements.txt` — the same tree production uses — and as a side benefit the gate will now catch a breaking dependency release before the daily job does (I1) |
| **Ensemble blended model versions (D1 follow-on)** | Making version part of the write key means a statement can hold two sets of sentence rows for the same model. `compute_ensemble_scores` means across rows with no version filter, so the first 2.0 re-score would have been silently averaged with the 1.0 rows it replaced — every published number moves, and "1.0 is preserved" becomes false. `_latest_version_only` now keeps one build per `(statement_id, model)`, chosen by `model_scores.scored_at`. Verified a no-op on the real store: 4,291 scores, checksum `1916.6433413417`, zero rows moved |
| **Gemini/DeepSeek dropped short statements on a bad reply (B4)** | Only Claude retried a JSON parse failure on a short statement; the other two called once and let it raise, so one malformed response cost that model's score for the statement. Both had the 3-attempt retry on the chunked path only. Two smaller drifts fixed with it: only Claude scrubbed control characters from the text it *sent*, and only Gemini/DeepSeek could salvage a truncated reply — a truncated Claude response threw away every sentence already parsed |
| **Ensemble versioning (D1)** | Writes were destructive by `(statement_id, model)`, so re-scoring with a newer model rewrote history in place and made the series non-comparable across the break with nothing marking it. `model_version` and `prompt_version` are now part of the write key: the same build stays idempotent, a new build appends. A separate `ensemble_scores` table holds per-version doc scores, leaving `statements.doc_score` as the published series so nothing downstream changes |
| **Fed charset bug (B1)** | `requests` defaults `text/*` to ISO-8859-1 when no charset is declared. The Fed declares none, so every en-dash and curly quote became mojibake in 54 statements (2007–2026) — **and the models scored the corrupted text**. Fixed centrally in `BaseScraper._get`. Writing the guard test then exposed **six more paths** (`boi`, `bot`, `nbp` ×2, `rbnz` Wayback, `sarb`, plus the shared calendar probe) that read `.text` from their own `requests.get` and bypassed it — all now corrected, with a contract test that fails if a new one appears |
| `fetch_cb` returned `(0,0)` for both "nothing new" and "scraper dead" | The core blind spot. Detection went from **45–136 days to ~3 days** |
| **Auto-blocking of scheduled meetings** | `SNB_MPC_STATEMENT_20260618` and `BCR_RATE_DECISION_20260710` were permanently blocked (2026-07-05 / 07-12). A blocked ID is rejected before any scraper fix can matter, so both stayed stale for months while their scrapers were "being fixed". Now a calendar-listed meeting can never be auto-blocked, and a test asserts the invariant |
| `alert.py` had no `if: always()` | `update.py` exits 1 on the cost guard, so the runs most needing an alert sent none |
| `_compute_staleness` skipped CBs with zero statements | A scraper broken from day one could never alert |
| Model failure counter required `ok == 0` | A model scoring 1 of 40 reset its own counter |
| Cost-guard path wrote an empty `stale_cbs` | Erased staleness state on exactly the runs that tripped it |
| `nbp.py` never declared `CB` / `DOC_TYPE` | Any shared `BaseScraper` helper using `self.CB` failed for NBP alone. Caught by the new contract tests |
| New `_session` property collided with `rbi.py`'s attribute | Broke RBI's constructor outright. Caught by the new contract tests |
| BCR manual-refresh URL pointed at **Peru** (`bcrp.gob.pe`) | Scraper targets Colombia. Anyone refreshing the calendar would have transcribed the wrong country's dates |
| MNB non-decision Wednesday meetings | `_merge_dates` is append-only, so the 2026-08-01 refresh re-added dates deleted in June. Filter now lives in the fetcher |
| Weekend calendar entries counted as missed meetings | BOK's Sunday entry became a false positive the moment real blocks cleared |
| `dashboard/app.py` `_CORE_MODELS` `NameError` | Both internal drill-down panels had been dead since the initial commit |
| `loadCbSentences` cached `{}` on a failed fetch | Poisoned the cache for the whole session; the UI would claim a CB had no sentence data forever |
| Explorer render race | A slow response for a CB the user navigated away from could overwrite the current view |
| `BaseScraper._get` had no retry | One transient 503 silently truncated a whole discovery pass |

---

## Conventions

- **Add** an entry the moment something is found, even if not fixed. A known
  issue with no owner still beats an unknown one.
- **Move** to *Fixed this cycle* with a one-line note on why it mattered, not
  just what changed.
- Cross-reference GitHub issues where one exists (currently: **#14** for the
  NBP local run).
- Re-check `Last reviewed` when touching this file.
